services:
  x402-video:
    networks:
      - projects
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DEMO_MODE=false
    restart: unless-stopped
  
networks:
  projects:
    external: true